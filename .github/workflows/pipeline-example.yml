name: Build Example Pipeline

on:
  push:
    branches:
      - devops
  pull_request:
    branches:
      - devops

jobs:
  build:
    # runs-on: ubuntu-latest
    checkouts:
      - name: Show simple message
        uses: rafaelgoncalvesmatos/DevOpsPipelineTemplates/.github/workflows/infra-deploy.yml@devops
      # - name: Checkout
      #   uses: actions/checkout@v2

      # - name: Show simple message
      #   shell: bash
      #   run: echo "Hello World!"  
      